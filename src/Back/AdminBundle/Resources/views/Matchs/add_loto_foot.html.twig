{% extends 'BackAdminBundle::layout_match.html.twig' %}
{% block title %} {{ parent() }} {% endblock %}
{% block ariane %} {{ parent() }} {% endblock %}

{% block page %}

    {#<div class="box-header-response">
        {{ 'Listes des Matchs du jour'|trans }}
        {% include '@BackAdmin/Matchs/sous_menu.html.twig' %}
    </div>#}
    <div class="sous-menu-page">
        {% include "BackAdminBundle:Matchs:sous_menu_lotofoot.html.twig" %}
    </div>
    <div class="col-lg-12">
       <div class="col-lg-3"></div>
        <div class="col-lg-6">
            {#<form action="{{ path('add_loto_foot') }}" method="post">#}
            {{ form_start(form) }}
                <table>
                    <tr>
                        <td>{{ 'Numéro'|trans }}</td>
                        <td>
                           {{ form_widget(form.numero) }}
                            {{ form_errors(form.numero) }}
                        </td>
                    </tr>
                    <tr>
                        <td>{{ 'Loto Foot'|trans }}</td>
                        <td>
                            {{ form_widget(form.typeLotoFoot) }}
                            {{ form_errors(form.typeLotoFoot) }}
                        </td>
                    <tr>
                        <td>{{ 'Fin de validation'|trans }}</td>
                        <td>
                            {{ form_widget(form.finValidation) }}
                            {{ form_errors(form.finValidation) }}
                        </td>
                    </tr
                    </tr>
                    <tr>
                        <td colspan="2">
                            <input type="submit" value="{{ 'Enregistrer' }}"/>
                        </td>
                    </tr>
                </table>
            {{ form_end(form) }}
            {#</form>#}
        </div>
        <div class="col-lg-3"></div>
    </div>
{% endblock %}