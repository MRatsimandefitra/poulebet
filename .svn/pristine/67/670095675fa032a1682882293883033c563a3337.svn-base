{% extends 'BackAdminBundle::layout.html.twig' %}
{% block title %} {{ parent() }} {% endblock %}
{% block ariane %} {{ parent() }} {% endblock %}

{% block page %}

    <div class="col-lg-12">
        <div class="box-header-response">
            {{ 'Gestion Emails '|trans }} :
            <div class="link">
                {#{% if is_granted('ROLE_SUPER_ADMIN') %}#}
                <a href="{{ path('index_admin_parameter') }}">{{ 'Parametres Email'|trans }}</a> |
                {#{% endif %}#}
                <a href="{#{{ path('export_csv_utilisateur') }}#}">{{ 'Ajout Email'|trans }}</a>
            </div>
            <div class="clear">

            </div>
        </div>
    </div>
<div class="box-response">
    <div class="col-lg-12">
        <form action="{{ path('index_admin_email') }}" method="post">
            {{ 'Ajouter une nouvelle adresse email : '|trans }}
            <input type="email" name="email" /> {{ '@poulebet.com'|trans }}
            <input type="submit" value="{{ 'Créer'|trans }}" />
         </form>
    </div>
    <div class="col-lg-12">
        {% for flashMessage in app.session.flashbag.get('error') %}

            {{ flashMessage }}

        {% endfor %}

        {{ form_start(form) }}

            {{ 'Gerer les mots de passe des adresses existantes'|trans }}
        {#   <div class="col-lg-4">#}
               {{ form_widget(form.email) }}
           {#</div>
            <div class="col-lg-4">#}
                {{ form_widget(form.password) }}

        <input type="submit" value="{{ 'send'|trans }}"/>
            {#</div>#}
        {{ form_end(form) }}
    </div>
</div>
{% endblock %}
