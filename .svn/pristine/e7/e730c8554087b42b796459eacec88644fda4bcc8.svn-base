{% trans_default_domain 'FOSUserBundle' %}

<form  class="form_login" action="{{ path('fos_user_resetting_send_email') }}" method="POST">
    <div>
        <h3>{{ 'resetting.request.title'|trans }}</h3>
    </div>
    <div>
        {% if invalid_username is defined %}
            <p>{{ 'resetting.request.invalid_username'|trans({'%username%': invalid_username}) }}</p>
        {% else %} 
            <p>{{ 'resetting.request.libelle1'|trans }}</p>
            <p>{{ 'resetting.request.libelle2'|trans }}</p>
        {% endif %}
    </div>
    <div>
        <input type="text" id="username" name="username" required="required" />
    </div>    
    <div>
        <input type="submit" value="{{ 'resetting.request.submit'|trans }}" />
        <input type="button" value="{{ 'resetting.request.cancel'|trans }}" />
    </div>
</form>
